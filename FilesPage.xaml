<Page
    x:Class="UnderwaterHockeyTimer.FilesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UnderwaterHockeyTimer"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer FontFamily="Consolas" FontSize="18">
        <StackPanel Background="#414141" Padding="20" Spacing="10">
            <TextBlock Text="File Output Settings"/>
            <TextBlock Text="Click to display file location" Margin="0,20,0,0"/>
            <Button x:Name="btnFileLocation" Content="Show File Location" IsTabStop="False"/>
            <TextBlock Text="Check to enable" Margin="0,20,0,0"/>
            <CheckBox x:Name="chkFilesEnabled" Content="Files Enabled" IsTabStop="False"/>
            <TextBlock Text="Check to enable" Margin="0,20,0,0"/>
            <CheckBox x:Name="chkGoalTextOutput" Content="Goals" IsTabStop="False"/>
            <CheckBox x:Name="chkGameNumberTextOutput" Content="Game No" IsTabStop="False"/>
            <CheckBox x:Name="chkGameTextOutput" Content="Game Half Text" IsTabStop="False"/>
            <CheckBox x:Name="chkGameTimeOutput" Content="Game Time" IsTabStop="False"/>
        </StackPanel>
    </ScrollViewer>
</Page>
